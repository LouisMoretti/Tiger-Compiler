let
    type  rectype = {age : int}
    type  recreccy = {name: string, age : int}
    var rec1 : rectype := rectype {age=1000}
    var rec2 : recreccy := recreccy {name="hello", age=1000}
    var b := 2
in
    rec1;
    rec2;
    gc_run();
    b
end