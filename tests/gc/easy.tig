let
        type myint = int
        type  arrtype = array of myint

        var arr1 : arrtype := arrtype [10] of 0
        var b = 2
        var arr2 : arrtype := arrtype [10] of 3
in
    (
        arr1;
        arr2;

        gc_run();

        arr1
    );
    arr1
end
